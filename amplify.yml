version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - streamlit run app.py
      artifacts:
        baseDirectory: '.'
        files:
          - '**/*'
    cache:
      paths:
        - .venv/**/*
        - .streamlit/**
        - .cache/**/*

